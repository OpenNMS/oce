# Use this file for docker images/tags that are fixed to a specific version and aren't changing
# These will be eligible for caching in the CI system

helm=opennms/helm:2.0.0
kafka=spotify/kafka
postgres=postgres:9.6.1
